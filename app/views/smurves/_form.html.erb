<%= form_for(@smurf) do |f| %>
  <% if @smurf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smurf.errors.count, "error") %> prohibited this smurf from being saved:</h2>

      <ul>
      <% @smurf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
